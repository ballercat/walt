import { assert: Assert } from 'env';
type Assert = (i32, i32, i32) => void;

type Nested = { z: i32 };
type Struct = { x: i32, nested: Nested, list: i32[] };

const memory: Memory = { initial: 1 };

export function run() {
  const s: Struct = 1024;
  s.list[0] = 0xff;

  // s.nested = sizeof(Struct) + 1;

  assert('chained subscripts', 0xff, s.list[0]);
}

